age = raw_input("How old are you? ")
height = raw_input("How tall are you? ")
weight = raw_input("How much do you weigh? ")

print "So, you're %r old, %r tall and %r heavy." % (
    age, height, weight)


#Study Drills
# Pydoc
# The pydoc module automatically generates documentation from Python modules.
# The documentation can be presented as pages of text on the console, served
# to a Web browser, or saved to HTML files.

# open(...)  -  opens a file using the file() type.  Preferred way to
#               open a file.

# file(object) - Opens a file.  The mode can be 'r', 'w', or 'a' form
#                reading(default), writing, or appending. The file will
#                be created if it doesn't exist when opened for writing or
#                appending; it will be truncated when opened of writing.

#os  OS routines for NT or Posix depending on what system we're on.
# POSIX (Portable Operating System Interface) is a family of standards specified
# by the IEEE Computer Society for maintaining compatibility between operating
# systems. POSIX defines at application programming interface (API), along
# with command line shells and utility interfaces, for software compatibility
# with variants of Unix and other operating systems.

#SYS - this module provides access to some objects used of maintained by the
#       interpreter and to functions that interact strongly with the
#       interpreter.
